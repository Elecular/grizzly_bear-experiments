apiVersion: apps/v1
kind: Deployment
metadata:
  name: experiments-deployment
  labels:
    app: experiments
spec:
  replicas: 3
  selector:
    matchLabels:
      app: experiments
  template:
    metadata:
      labels:
        app: experiments
    spec:
      containers:
      - name: experiments
        image: XXXX
        ports:
        - containerPort: 3000