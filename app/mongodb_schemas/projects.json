{
    "bsonType": "object",
    "required": ["projectName", "ownerId"],
    "properties": {
        "projectName": {
            "bsonType": "string",
            "description": "Name of the project"
        },
        "ownerId": {
            "bsonType": "string",
            "description": "Id of the project owner"
        }
    }
}
